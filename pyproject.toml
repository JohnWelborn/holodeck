[project]
name = "holodeck"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pyyaml",
]

[project.optional-dependencies]
backend = [
    "llama-cpp-python",
    "uvicorn",
    "fastapi",
    "pydantic-settings",
    "sse-starlette",
    "starlette-context",
]
